# Changes require a restart to take effect.

# Ensure bins are in the path
export PATH=$HOME/.dots/bin:$PATH:$HOME/.local/bin

export TERMINAL=xdg-terminal-exec
export EDITOR=nvim
export ZDOTDIR="$HOME/.config/zsh"
